[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install flask flask-cors gunicorn"]

[start]
cmd = "gunicorn server:app --workers 1 --bind 0.0.0.0:$PORT --timeout 120 --preload"
